service : {
  getAttendees: () -> (vec record {
                             text;
                             nat;
                           }) query;
  registerAttendance: (text, nat) -> (bool);
}
